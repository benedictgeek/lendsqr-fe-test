export const VerticalSpacer = ({ size }: { size: number }) => {
  return <div style={{ height: `${size}px` }}></div>;
};

export const HorizontalSpacer = ({ size }: { size: number }) => {
  return <div style={{ width: `${size}px` }}></div>;
};
